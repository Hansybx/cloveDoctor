export default interface recommond {
    id?: number;
    drugId: number;
    drugName: string;
    drugImg: string;
}

export default interface recommondDrug {
    drugId: number;
    drugName: string;
}